po_fully_convert = {
	category = 6
	power_projection = humiliated_rival
	power_cost_base = 1
	prestige_base = 0.15
	ae_base = 0.1
	warscore_cost = {
		all_provinces = 100
	}
	warscore_cap = 100
	requires_is_allowed = yes
	is_make_subject = no
	effect = {
		FROM = {
			change_religion = ROOT
			capital_scope = {
				change_religion = ROOT
			}
			add_country_modifier = {
				name = recent_full_conversion
				duration = 5475
			}
			kill_ruler = yes
			kill_consort = yes
			kill_heir = {
			}
			define_ruler = {
				culture = FROM
				religion = ROOT
			}
		}
	}
	ai_weight = {
		export_to_variable = {
			variable_name = ai_value
			value = 50
		}
	}
}
