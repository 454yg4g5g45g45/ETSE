# File: custom_client_state_decision.txt

country_decisions = {
    create_client_state_decision = {
        potential = {
            NOT = { has_country_modifier = client_state_creation_enabled }  # Decision only shows if modifier isn't active
        }
        allow = {
            # No requirements specified as requested
        }
        effect = {
            add_country_modifier = {
                name = "client_state_creation_enabled"
                duration = -1
            }
            custom_tooltip = CREATE_CLIENT_STATE_DECISION_TOOLTIP
        }
        ai_will_do = {
            factor = 1
        }
    }
}